import { useEffect } from 'react';
import hotkeys from 'hotkeys-js';
export var useHotkeys = function useHotkeys(key, cb, inputs) {
  useEffect(function () {
    hotkeys(key, cb);
    return function () {
      return hotkeys.unbind(key);
    };
  }, inputs);
};